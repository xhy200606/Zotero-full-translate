import Icon from './Icon'
import BrandLogo from './BrandLogo'
const items=[['overview','dashboard','总览'],['providers','services','翻译 API'],['clients','devices','Zotero 客户端'],['jobs','tasks','任务'],['security','settings','账户安全']]
function NavItem({item,current,onChange,compact=false}){const [id,icon,label]=item;return <button className={`nav-item ${current===id?'active':''}`} onClick={()=>onChange(id)} aria-current={current===id?'page':undefined}><span className="nav-indicator"><Icon name={icon} size={compact?23:24}/></span><span className="nav-label">{label}</span></button>}
export default function Navigation({view,onChange}){return <><aside className="md-nav-drawer"><div className="brand-block"><BrandLogo/><div><strong>Zotero Full Translate</strong><span>用户中心</span></div></div><nav>{items.map(x=><NavItem key={x[0]} item={x} current={view} onChange={onChange}/>)}</nav><div className="drawer-footer"><span>Account Workspace</span><small>Cloud 2.5.2</small></div></aside><aside className="md-nav-rail"><BrandLogo compact/><nav>{items.map(x=><NavItem key={x[0]} item={x} current={view} onChange={onChange}/>)}</nav></aside><nav className="md-nav-bar" style={{gridTemplateColumns:'repeat(5,1fr)'}}>{items.map(x=><NavItem key={x[0]} item={x} current={view} onChange={onChange} compact/>)}</nav></>}
